<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?= htmlspecialchars($title ?? 'HeArt') ?></title>

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <link rel="stylesheet" href="<?= ASSETS_URL ?>/css/global.css">
    <link rel="stylesheet" href="<?= ASSETS_URL ?>/css/components.css">
    <link rel="stylesheet" href="<?= ASSETS_URL ?>/css/responsive.css">

    <?php if (isset($extraCss)): ?>
    <link rel="stylesheet" href="<?= ASSETS_URL ?>/css/pages/<?= $extraCss ?>.css">
    <?php endif; ?>
</head>

<body>

    <nav class="navbar">
        <a href="<?= BASE_URL ?>/" class="navbar-brand">HeArt</a>

        <div class="navbar-links">
            <?php if (isset($_SESSION['user_id'])): ?>
            <a href="<?= BASE_URL ?>/albums/create">+ Créer</a>
            <a href="<?= BASE_URL ?>/albums">Mes albums</a>
            <a href="<?= BASE_URL ?>/logout">Déconnexion</a>
            <?php else: ?>
            <a href="<?= BASE_URL ?>/login">Connexion</a>
            <a href="<?= BASE_URL ?>/register">S'inscrire</a>
            <?php endif; ?>
        </div>
    </nav>

    <main class="main-content">